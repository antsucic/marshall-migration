INSERT INTO staging.locations_users
(
    legacy_location_id
    , legacy_source
    , legacy_user_id
)
SELECT
    entities."Location_Id"
    , '#{source}'
    , entities."Id"
FROM
    "#{source}"."Entities" entities
